[project]
name = "django-gdpr-cookie-consent-demo-project"
version = "0.1.0"
description = "Demo project for the django-gdpr-cookie-consent package"
readme = "README.md"
license = {file = "LICENSE.md"}
requires-python = ">=3.10"

[build-system]
requires = ["uv_build >= 0.10.0, <0.11.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "demo_project"
module-root = ""
